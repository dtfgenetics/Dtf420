(()=>{'use strict';
const LEVELS=window.SEED_ASCENT_LEVELS||[],canvas=document.getElementById('game'),ctx=canvas.getContext('2d'),W=canvas.width,H=canvas.height,T=48;ctx.imageSmoothingEnabled=false;
const ui={level:document.getElementById('levelLabel'),score:document.getElementById('scoreLabel'),tri:document.getElementById('trichomeLabel'),health:document.getElementById('healthLabel'),power:document.getElementById('powerLabel')};
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),overlap=(a,b)=>a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y,rand=(a,b)=>a+Math.random()*(b-a);
const unlocked=clamp(parseInt(localStorage.getItem('seedAscentUnlocked')||'1',10)||1,1,LEVELS.length||1);
const game={mode:'title',levelIndex:0,selectedLevel:0,unlocked,score:0,trichomes:0,health:3,maxHealth:3,cameraX:0,shake:0,level:null,checkpoint:null,power:'NONE',powerTimer:0,shield:false,best:parseInt(localStorage.getItem('seedAscentRetroBest')||'0',10)||0};
const input={left:false,right:false,jumpHeld:false,jumpPressed:false,jumpReleased:false,run:false};
const player={x:96,y:300,w:34,h:42,vx:0,vy:0,prevX:0,prevY:0,grounded:false,coyote:0,jumpBuffer:0,doubleUsed:false,facing:1,invuln:0,anim:0};
let solids=[],blocks=[],coins=[],powerups=[],enemies=[],checkpoints=[],particles=[],movingPlatforms=[],exitGate=null,boss=null,audioCtx=null;
function audio(){if(!audioCtx){try{audioCtx=new(window.AudioContext||window.webkitAudioContext)()}catch{}}if(audioCtx&&audioCtx.state==='suspended')audioCtx.resume().catch(()=>{});return audioCtx}
function beep(f=440,d=.08,type='square',vol=.035){const a=audio();if(!a)return;try{const o=a.createOscillator(),g=a.createGain();o.type=type;o.frequency.value=f;g.gain.value=vol;o.connect(g);g.connect(a.destination);o.start();g.gain.exponentialRampToValueAtTime(.001,a.currentTime+d);o.stop(a.currentTime+d)}catch{}}
const sounds={jump(){beep(330,.06);setTimeout(()=>beep(470,.05),55)},coin(){beep(820,.05);setTimeout(()=>beep(1120,.04),45)},power(){[440,660,880].forEach((f,i)=>setTimeout(()=>beep(f,.07),i*70))},stomp(){beep(160,.08)},hit(){beep(110,.14,'sawtooth',.05)},goal(){[523,659,784,1046].forEach((f,i)=>setTimeout(()=>beep(f,.1),i*90))}};
function sync(){ui.level.textContent=game.level?game.level.world:LEVELS[game.selectedLevel]?.world||'1-1';ui.score.textContent=String(game.score).padStart(6,'0');ui.tri.textContent=game.trichomes;ui.health.textContent=game.health;ui.power.textContent=game.shield?'SHIELD':game.power}
function score(n){game.score+=n;if(game.score>game.best){game.best=game.score;localStorage.setItem('seedAscentRetroBest',String(game.best))}sync()}
function particle(x,y,color,n=8){for(let i=0;i<n;i++)particles.push({x,y,vx:rand(-3,3),vy:rand(-4,-1),life:rand(22,48),color,s:rand(2,5)})}
function clearWorld(){solids=[];blocks=[];coins=[];powerups=[];enemies=[];checkpoints=[];particles=[];movingPlatforms=[];exitGate=null;boss=null}
function buildLevel(data){clearWorld();for(const [x,w,y=456] of data.grounds)solids.push({x,y,w,h:H-y+80,type:'ground'});for(const [x,y,w,type='platform'] of data.platforms)solids.push({x,y,w,h:24,type});for(const [x,y,payload] of data.blocks)blocks.push({x,y,w:T,h:T,payload,used:false,bump:0});for(const [x,y,count,spacing=34] of data.coinLines)for(let i=0;i<count;i++)coins.push({x:x+i*spacing,y,w:20,h:20,bob:Math.random()*6.28});for(const [x,y,type,range] of data.enemies)addEnemy(x,y,type,range);for(const [x,y,w,axis,range,speed] of data.moving)movingPlatforms.push({x,y,w,h:18,startX:x,startY:y,axis,range,speed,phase:0,dx:0,dy:0});for(const [x,y] of data.checkpoints)checkpoints.push({x,y,w:30,h:74,active:false});exitGate={x:data.exit[0],y:data.exit[1],w:data.exit[2],h:data.exit[3]};if(data.boss){const [x,y,min,max]=data.boss;boss={x,y,w:78,h:58,min,max,dir:-1,vx:1.4,vy:0,hp:8,maxHp:8,invuln:0,phase:0,dead:false}}}
function addEnemy(x,y,type='APHID',range=150){const c={APHID:[34,28,1.1,1],MITE:[28,22,1.8,1],CATERPILLAR:[48,30,.7,2],GNAT:[30,24,1.3,1]}[type];enemies.push({x,y,w:c[0],h:c[1],type,s:c[2],hp:c[3],maxHp:c[3],dir:-1,startX:x,range,phase:Math.random()*6.28})}
function loadLevel(i){game.levelIndex=clamp(i,0,LEVELS.length-1);game.selectedLevel=game.levelIndex;game.level=LEVELS[game.levelIndex];buildLevel(game.level);game.cameraX=0;game.power='NONE';game.powerTimer=0;game.shield=false;game.health=game.maxHealth;game.checkpoint=null;Object.assign(player,{x:96,y:340,vx:0,vy:0,grounded:false,doubleUsed:false,invuln:0});sync()}
function startSelected(){audio();if(game.mode==='title'||game.mode==='gameOver'){game.score=0;game.trichomes=0}loadLevel(game.selectedLevel);game.mode='playing';canvas.focus()}
function respawn(){game.power='NONE';game.powerTimer=0;game.shield=false;const cp=game.checkpoint;Object.assign(player,{x:cp?cp.x+30:96,y:cp?cp.y:320,vx:0,vy:0,invuln:100});game.cameraX=clamp(player.x-W*.35,0,game.level.width-W);sync()}
function gameOver(){game.mode='gameOver';game.power='NONE';game.powerTimer=0;game.shield=false;sounds.hit();sync()}
function fallDeath(){if(game.mode!=='playing')return;game.health--;sounds.hit();game.shake=18;if(game.health<=0)gameOver();else respawn()}
function hurt(){if(player.invuln>0||game.power==='RUSH')return;if(game.shield){game.shield=false;player.invuln=100;game.shake=12;sounds.hit();sync();return}game.health--;player.invuln=120;player.vy=-8;player.vx=-player.facing*5;game.shake=14;sounds.hit();sync();if(game.health<=0)gameOver()}
function collectPower(type){if(type==='SHIELD'){game.shield=true;game.power='NONE';game.powerTimer=0}else{game.power=type;game.powerTimer=type==='RUSH'?600:900}score(500);sounds.power()}
function hitBlock(b){if(b.used)return;b.used=true;b.bump=10;sounds.coin();if(b.payload==='TRI'){game.trichomes+=5;score(250);particle(b.x+24,b.y,'#ffd45b',10)}else powerups.push({x:b.x+10,y:b.y-36,w:28,h:28,type:b.payload,vy:0,vx:b.payload==='LIGHT'?1.2:0});sync()}
function updateMoving(){for(const p of movingPlatforms){const ox=p.x,oy=p.y;p.phase+=p.speed*.022;const d=Math.sin(p.phase)*p.range;p.axis==='x'?p.x=p.startX+d:p.y=p.startY+d;p.dx=p.x-ox;p.dy=p.y-oy}}
function collideWorld(){player.grounded=false;for(const s of solids.concat(movingPlatforms,blocks)){const pb=player.prevY+player.h,pt=player.prevY,pr=player.prevX+player.w,pl=player.prevX;if(!overlap(player,s))continue;if(player.vy>=0&&pb<=s.y+5){player.y=s.y-player.h;player.vy=0;player.grounded=true;player.doubleUsed=false;if(movingPlatforms.includes(s))player.x+=s.dx}else if(player.vy<0&&pt>=s.y+s.h-5){player.y=s.y+s.h;player.vy=0;if(blocks.includes(s))hitBlock(s)}else if(player.vx>0&&pr<=s.x+5){player.x=s.x-player.w;player.vx=0}else if(player.vx<0&&pl>=s.x+s.w-5){player.x=s.x+s.w;player.vx=0}}}
function updatePlayer(){player.prevX=player.x;player.prevY=player.y;player.anim+=Math.abs(player.vx)*.08;const boost=game.power==='LIGHT',max=input.run?(boost?8.2:7):(boost?6.3:5.2),accel=input.run?.72:.58;if(input.left){player.vx-=accel;player.facing=-1}else if(input.right){player.vx+=accel;player.facing=1}else player.vx*=player.grounded?.76:.92;player.vx=clamp(player.vx,-max,max);player.jumpBuffer=Math.max(0,player.jumpBuffer-1);player.coyote=player.grounded?7:Math.max(0,player.coyote-1);if(input.jumpPressed)player.jumpBuffer=8;if(player.jumpBuffer>0){if(player.coyote>0){player.vy=boost?-14.5:-13.2;player.coyote=0;player.jumpBuffer=0;player.doubleUsed=false;sounds.jump();particle(player.x+17,player.y+42,'#8be88f',5)}else if(!player.doubleUsed&&player.vy>-9){player.vy=boost?-13:-11.6;player.doubleUsed=true;player.jumpBuffer=0;sounds.jump();particle(player.x+17,player.y+42,'#ffd45b',10)}}if(input.jumpReleased&&player.vy<-4)player.vy*=.52;player.vy=Math.min(player.vy+.68,15);player.x=clamp(player.x+player.vx,0,game.level.width-player.w);player.y+=player.vy;collideWorld();if(player.invuln>0)player.invuln--;if(player.y>H+220)fallDeath();input.jumpPressed=input.jumpReleased=false}
function updateCoins(){coins=coins.filter(c=>{c.bob+=.08;if(overlap(player,{x:c.x-4,y:c.y-4,w:28,h:28})){game.trichomes++;score(100);sounds.coin();particle(c.x+10,c.y+10,'#f5f7ff',6);return false}return true})}
function updatePowerups(){powerups=powerups.filter(p=>{p.vy=Math.min(p.vy+.45,8);p.x+=p.vx;p.y+=p.vy;for(const s of solids)if(overlap(p,s)&&p.vy>=0){p.y=s.y-p.h;p.vy=0}if(overlap(player,p)){collectPower(p.type);return false}return true})}
function updateEnemies(){for(const e of enemies){if(e.type==='GNAT'){e.phase+=.04;e.x+=e.s*e.dir;e.y+=Math.sin(e.phase)*.7;if(Math.abs(e.x-e.startX)>e.range)e.dir*=-1}else{e.x+=e.s*e.dir;if(Math.abs(e.x-e.startX)>e.range)e.dir*=-1;for(const s of solids){if(overlap({x:e.x,y:e.y+e.h,w:e.w,h:5},s)){e.y=s.y-e.h;break}}}if(overlap(player,e)){const stomp=player.vy>1&&player.prevY+player.h<=e.y+10;if(stomp){e.hp--;player.vy=-9.5;sounds.stomp();particle(e.x+e.w/2,e.y,'#ef765a',10);if(e.hp<=0){e.dead=true;score(e.type==='CATERPILLAR'?350:200)}else e.dir*=-1}else if(game.power==='RUSH'){e.dead=true;score(250);particle(e.x,e.y,'#ffd45b',14)}else hurt()}}enemies=enemies.filter(e=>!e.dead)}
function updateCheckpoints(){for(const cp of checkpoints)if(!cp.active&&overlap(player,cp)){checkpoints.forEach(c=>c.active=false);cp.active=true;game.checkpoint={x:cp.x,y:cp.y-20};score(250);sounds.power()}}
function updateBoss(){if(!boss||boss.dead)return;boss.phase++;if(boss.invuln>0)boss.invuln--;const rage=1+(1-boss.hp/boss.maxHp)*.8;boss.x+=boss.vx*boss.dir*rage;if(boss.x<boss.min||boss.x+boss.w>boss.max){boss.x=clamp(boss.x,boss.min,boss.max-boss.w);boss.dir*=-1}if(boss.phase%150===0&&boss.vy===0)boss.vy=-9;boss.vy=Math.min(boss.vy+.55,12);boss.y+=boss.vy;for(const s of solids){if(overlap(boss,s)&&boss.vy>=0){boss.y=s.y-boss.h;boss.vy=0;break}}if(overlap(player,boss)){const stomp=player.vy>1&&player.prevY+player.h<=boss.y+16;if(stomp&&boss.invuln<=0){boss.hp--;boss.invuln=35;player.vy=-11;sounds.stomp();game.shake=16;particle(boss.x+boss.w/2,boss.y,'#e96b78',24);score(750);if(boss.hp<=0){boss.dead=true;score(5000);sounds.goal();particle(boss.x+boss.w/2,boss.y+20,'#ffd45b',50)}}else if(game.power==='RUSH'&&boss.invuln<=0){boss.hp--;boss.invuln=45;game.shake=12;particle(boss.x,boss.y,'#ffd45b',18);if(boss.hp<=0){boss.dead=true;score(5000);sounds.goal()}}else if(boss.invuln<=0)hurt()}}

function updateExit(){if(boss&&!boss.dead)return;if(exitGate&&overlap(player,exitGate)){game.mode='levelComplete';score(2000+game.health*250);sounds.goal();const next=game.levelIndex+2;if(next>game.unlocked&&next<=LEVELS.length){game.unlocked=next;localStorage.setItem('seedAscentUnlocked',String(next))}}}
function step(){if(game.mode!=='playing')return;updateMoving();updatePlayer();updateCoins();updatePowerups();updateEnemies();updateCheckpoints();updateBoss();updateExit();if(game.powerTimer>0&&--game.powerTimer<=0){game.power='NONE';sync()}for(const p of particles){p.x+=p.vx;p.y+=p.vy;p.vy+=.18;p.life--}particles=particles.filter(p=>p.life>0);const target=clamp(player.x-W*.38,0,game.level.width-W);game.cameraX+=(target-game.cameraX)*.11;if(game.shake>0)game.shake*=.82;sync()}
function px(x,y,w,h,c){ctx.fillStyle=c;ctx.fillRect(Math.floor(x),Math.floor(y),Math.ceil(w),Math.ceil(h))}
function background(){const t=game.level||LEVELS[game.selectedLevel],g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,t.sky[0]);g.addColorStop(1,t.sky[1]);ctx.fillStyle=g;ctx.fillRect(0,0,W,H);const cam=game.cameraX||0;ctx.globalAlpha=.22;ctx.fillStyle='#143525';for(let i=-1;i<9;i++){const x=i*260-(cam*.16%260);ctx.beginPath();ctx.moveTo(x,H);ctx.lineTo(x+130,250+(i%2)*40);ctx.lineTo(x+260,H);ctx.fill()}ctx.globalAlpha=.13;ctx.fillStyle='#0b2b17';for(let i=-1;i<18;i++){const x=i*120-(cam*.32%120);ctx.fillRect(x+55,330,10,130);ctx.beginPath();ctx.ellipse(x+45,345,25,11,-.4,0,Math.PI*2);ctx.ellipse(x+76,348,25,11,.4,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1}
function drawPlayer(){if(player.invuln>0&&Math.floor(player.invuln/5)%2===0)return;ctx.save();ctx.translate(player.x+17,player.y+21+(player.grounded&&Math.abs(player.vx)>1?Math.sin(player.anim)*2:0));if(player.facing<0)ctx.scale(-1,1);ctx.fillStyle=game.power==='RUSH'?'#db8fff':game.power==='LIGHT'?'#d9db75':'#79985e';ctx.beginPath();ctx.ellipse(0,3,16,19,-.15,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#e6ffd455';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,3,7,15,-.15,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#fff';ctx.fillRect(5,-5,4,4);ctx.fillStyle='#172016';ctx.fillRect(7,-4,2,2);ctx.strokeStyle='#6fd17d';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(0,-14);ctx.quadraticCurveTo(-3,-23,2,-30);ctx.stroke();ctx.fillStyle='#69c875';ctx.beginPath();ctx.ellipse(-4,-30,9,4,-.4,0,Math.PI*2);ctx.ellipse(6,-28,9,4,.4,0,Math.PI*2);ctx.fill();if(game.shield){ctx.strokeStyle='#8ce2ff';ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,27,0,Math.PI*2);ctx.stroke()}ctx.restore()}
function world(){const ox=-game.cameraX+(game.shake?rand(-game.shake,game.shake):0),t=game.level;ctx.save();ctx.translate(ox,0);for(const s of solids){let c=t.ground;if(s.type==='stone')c='#5a6067';if(s.type==='brick')c='#74513f';if(s.type==='ice')c='#8ec6d7';px(s.x,s.y,s.w,s.h,c);px(s.x,s.y,s.w,5,t.accent)}for(const m of movingPlatforms){px(m.x,m.y,m.w,m.h,'#557c5a');px(m.x,m.y,m.w,4,'#a4e9a8')}for(const b of blocks){const y=b.y-(b.bump>0?Math.sin(b.bump/10*Math.PI)*8:0);if(b.bump>0)b.bump--;px(b.x,y,b.w,b.h,b.used?'#6a604b':'#b48a3b');px(b.x+4,y+4,b.w-8,b.h-8,b.used?'#514a3e':'#d0a94f');ctx.fillStyle=b.used?'#887e69':'#fff2ad';ctx.font='bold 24px monospace';ctx.fillText(b.used?'·':'?',b.x+16,y+32)}for(const c of coins){const y=c.y+Math.sin(c.bob)*5;ctx.fillStyle='#f4f7ff';ctx.beginPath();ctx.arc(c.x+10,y+10,9,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#bde9ff';ctx.lineWidth=3;ctx.stroke()}for(const p of powerups){ctx.fillStyle={LIGHT:'#ffe36d',SHIELD:'#8ce2ff',RUSH:'#e79cff'}[p.type];ctx.beginPath();ctx.arc(p.x+14,p.y+14,14,0,Math.PI*2);ctx.fill();ctx.fillStyle='#142016';ctx.font='bold 15px monospace';ctx.fillText(p.type==='LIGHT'?'☀':p.type==='SHIELD'?'M':'R',p.x+7,p.y+20)}for(const cp of checkpoints){px(cp.x+12,cp.y,6,cp.h,cp.active?'#ffe36d':'#d5ded6');ctx.fillStyle=cp.active?'#ffe36d':'#79b781';ctx.beginPath();ctx.moveTo(cp.x+18,cp.y+5);ctx.lineTo(cp.x+55,cp.y+18);ctx.lineTo(cp.x+18,cp.y+32);ctx.fill()}if(exitGate){const locked=boss&&!boss.dead;px(exitGate.x+10,exitGate.y,8,exitGate.h,locked?'#75666a':'#d9e1d9');px(exitGate.x+18,exitGate.y+18,52,60,locked?'#552735':'#2b7137');px(exitGate.x+24,exitGate.y+24,40,48,locked?'#8c4558':'#82c987');ctx.fillStyle='#efffe8';ctx.font='bold 18px monospace';ctx.fillText(locked?'LOCK':'DTF',exitGate.x+21,exitGate.y+55)}if(boss&&!boss.dead){const flash=boss.invuln>0&&Math.floor(boss.invuln/4)%2===0;ctx.fillStyle=flash?'#fff':'#9b3248';ctx.beginPath();ctx.ellipse(boss.x+boss.w/2,boss.y+boss.h/2,boss.w/2,boss.h/2,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#d95f72';for(const sx of [10,30,50,68]){ctx.fillRect(boss.x+sx,boss.y-8,6,18)}ctx.fillStyle='#fff';ctx.fillRect(boss.x+20,boss.y+18,10,8);ctx.fillRect(boss.x+48,boss.y+18,10,8);px(boss.x,boss.y-16,boss.w,7,'#35151d');px(boss.x,boss.y-16,boss.w*(boss.hp/boss.maxHp),7,'#ff7c8f')}for(const e of enemies){const c=e.type==='MITE'?'#e95858':e.type==='CATERPILLAR'?'#7c61b9':e.type==='GNAT'?'#6ab5dc':'#c46b42';ctx.fillStyle=c;if(e.type==='GNAT'){ctx.beginPath();ctx.ellipse(e.x+e.w/2,e.y+e.h/2,e.w/2,e.h/2,0,0,Math.PI*2);ctx.fill()}else px(e.x,e.y,e.w,e.h,c);ctx.fillStyle='#fff';ctx.fillRect(e.x+7,e.y+8,5,5);ctx.fillRect(e.x+e.w-12,e.y+8,5,5);if(e.maxHp>1){px(e.x,e.y-8,e.w,4,'#391c25');px(e.x,e.y-8,e.w*(e.hp/e.maxHp),4,'#9cff83')}}for(const p of particles){ctx.globalAlpha=clamp(p.life/35,0,1);px(p.x,p.y,p.s,p.s,p.color)}ctx.globalAlpha=1;drawPlayer();ctx.restore()}
function overlay(title,sub,lines=[]){ctx.fillStyle='#000b';ctx.fillRect(0,0,W,H);ctx.textAlign='center';ctx.fillStyle='#8cf29a';ctx.font='900 48px monospace';ctx.fillText(title,W/2,190);ctx.fillStyle='#f0fff2';ctx.font='bold 20px monospace';ctx.fillText(sub,W/2,236);ctx.fillStyle='#a9c6ad';ctx.font='15px monospace';lines.forEach((l,i)=>ctx.fillText(l,W/2,285+i*28));ctx.textAlign='left'}
function draw(){background();if(game.level)world();if(game.mode==='title'){const l=LEVELS[game.selectedLevel];overlay('SEED ASCENT','Retro side-scrolling platform adventure',[`${l.world} · ${l.name}`,`Unlocked worlds: ${game.unlocked}/${LEVELS.length}`,'Run, stomp pests, find power-ups, hit checkpoints, reach the grow gate.','Press START or Space'])}else if(game.mode==='paused')overlay('PAUSED',`${game.level.world} · ${game.level.name}`,['Press P or PAUSE to resume']);else if(game.mode==='levelComplete'){const last=game.levelIndex===LEVELS.length-1;overlay(last?'HARVEST COMPLETE!':'LEVEL CLEAR!',`${game.level.world} · ${game.level.name}`,last?['You cleared all 12 stages across six grow worlds.','Boss systems, secrets and more worlds can build on this engine.','Press START to replay.']:[`Next: ${LEVELS[game.levelIndex+1].world} · ${LEVELS[game.levelIndex+1].name}`,'Press START to continue.'])}else if(game.mode==='gameOver')overlay('GAME OVER','The pests won this run.',[`Score ${String(game.score).padStart(6,'0')}`,`Best ${String(game.best).padStart(6,'0')}`,'Press START to begin a new run.'])}
function loop(){step();draw();requestAnimationFrame(loop)}
function jumpPress(){input.jumpPressed=true}function jumpRelease(){input.jumpReleased=true;input.jumpHeld=false}function togglePause(){if(game.mode==='playing')game.mode='paused';else if(game.mode==='paused')game.mode='playing'}function advance(){if(game.levelIndex<LEVELS.length-1){game.selectedLevel=game.levelIndex+1;loadLevel(game.selectedLevel);game.mode='playing'}else{game.mode='title';game.selectedLevel=0;game.level=null}}
function keyDown(e){const k=e.key,lower=k.toLowerCase();if(['ArrowLeft','ArrowRight','ArrowUp',' ','Shift','a','d','w','A','D','W','p','P'].includes(k))e.preventDefault();if(k==='ArrowLeft'||lower==='a')input.left=true;if(k==='ArrowRight'||lower==='d')input.right=true;if(k==='Shift')input.run=true;if(k===' '||k==='ArrowUp'||lower==='w'){if(!input.jumpHeld){input.jumpHeld=true;jumpPress()}if(game.mode==='title'||game.mode==='gameOver')startSelected();else if(game.mode==='levelComplete')advance()}if(lower==='p')togglePause()}
function keyUp(e){const k=e.key,lower=k.toLowerCase();if(k==='ArrowLeft'||lower==='a')input.left=false;if(k==='ArrowRight'||lower==='d')input.right=false;if(k==='Shift')input.run=false;if(k===' '||k==='ArrowUp'||lower==='w')jumpRelease()}
function hold(id,on,off){const b=document.getElementById(id);b.addEventListener('pointerdown',e=>{e.preventDefault();audio();try{b.setPointerCapture(e.pointerId)}catch{}on()});['pointerup','pointercancel','lostpointercapture'].forEach(t=>b.addEventListener(t,e=>{e.preventDefault();off()}))}
hold('leftBtn',()=>input.left=true,()=>input.left=false);hold('rightBtn',()=>input.right=true,()=>input.right=false);hold('runBtn',()=>input.run=true,()=>input.run=false);hold('jumpBtn',()=>{if(!input.jumpHeld){input.jumpHeld=true;jumpPress()}},jumpRelease);
document.getElementById('startBtn').addEventListener('click',()=>{audio();game.mode==='levelComplete'?advance():startSelected()});document.getElementById('pauseBtn').addEventListener('click',togglePause);document.getElementById('restartBtn').addEventListener('click',()=>{if(game.level){loadLevel(game.levelIndex);game.mode='playing'}else startSelected()});document.getElementById('prevBtn').addEventListener('click',()=>{if(game.mode==='playing')return;game.selectedLevel=(game.selectedLevel-1+game.unlocked)%game.unlocked;game.level=null;sync()});document.getElementById('nextBtn').addEventListener('click',()=>{if(game.mode==='playing')return;game.selectedLevel=(game.selectedLevel+1)%game.unlocked;game.level=null;sync()});canvas.addEventListener('pointerdown',()=>{audio();canvas.focus()});window.addEventListener('keydown',keyDown,{passive:false});window.addEventListener('keyup',keyUp,{passive:false});window.addEventListener('blur',()=>{input.left=input.right=input.run=input.jumpHeld=false});sync();loop();
})();
