# Academy mobile course completion note

The deterministic verifier checks the responsive contract tokens used by the course-detail stylesheet. Repository CI remains responsible for build, type, lint, and route regression checks.
